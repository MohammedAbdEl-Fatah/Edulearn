export enum RoleUSER {
    TEACHER = "teacher",
    STUDENT = "student",
    ADMIN = "admin",
}

export enum TokenType {
    ACCESS = "access",
    REFRESH = "refresh",
}
export enum EmailType {
    VERIFY = "verify",
    FORGOT = "forgot",
}